# Base URL of the NestJS API, used server-side only (BFF proxy + ISR fetches).
API_URL="http://localhost:3001"
# Shared secret for the /api/revalidate webhook; must match the API's
# REVALIDATE_SECRET, which the API validates (min 24 characters).
REVALIDATE_SECRET="change-me"
